language: ruby
rvm:
  - 2.5

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true
    - JEKYLL_ENV=production

# install:
#   - bundle install
# script:
#   - rake

cache:
  bundler: true
  directories:
    - $TRAVIS_BUILD_DIR/.jekyll-cache

branches:
  only:
    - master

notifications:
  email:
    on_success: never
    on_failure: change
